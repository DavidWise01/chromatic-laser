# Chromatic-32 Capacitance Well

A rebuilt static prototype: 32 fixed colored emitters, 8 per quadrant, firing into a central white capacitance well. The model advances by `.01` phase increments and treats the center as an accumulating/releasing well rather than a passive dot.

Open `index.html` locally. No dependencies. No network calls.
